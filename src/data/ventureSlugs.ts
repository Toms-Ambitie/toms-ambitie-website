// Venture slugs extracted for use in build-time tools (sitemap) that can't
// resolve image imports. Keep in sync with ventureDetails in ventures.ts.
export const ventureSlugs = [
  "emmastudio",
  "post-pilot",
  "plug-and-power",
] as const;
